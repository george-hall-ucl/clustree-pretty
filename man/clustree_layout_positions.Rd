% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layout.R
\name{clustree_layout_positions}
\alias{clustree_layout_positions}
\title{Get clustree layout positions}
\usage{
clustree_layout_positions(graph, clusterings, prefix, order_clusters = TRUE)
}
\arguments{
\item{graph}{\link[tidygraph:tbl_graph]{tidygraph::tbl_graph} object containing the tree graph}

\item{clusterings}{numeric matrix containing clustering information, each
column contains clustering at a separate resolution}

\item{prefix}{string indicating columns containing clustering information}

\item{order_clusters}{logical, whether to order clusters within each
resolution using \code{\link[=order_clustree_clusters]{order_clustree_clusters()}}. If \code{FALSE} clusters are placed
in sorted order.}
}
\value{
data.frame with \code{x} and \code{y} columns giving the position of each node,
in the order the nodes appear in \code{graph}
}
\description{
Calculate node positions for the clustree layout. Each resolution is placed
on its own row, with the lowest resolution at the top. Within a row clusters
are evenly spaced and centred, in the order given by
\code{\link[=order_clustree_clusters]{order_clustree_clusters()}}. Because spacing is constant, rows containing
more clusters are wider than those containing fewer.
}
\keyword{internal}
